{
  "name": "json-visualizer-extension",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:content": "vite build --mode content-js",
    "build:all": "vite build && vite build --mode content-js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.0.4"
  },
  "dependencies": {
    "@components-1812/json-visualizer": "^0.0.2"
  }
}
