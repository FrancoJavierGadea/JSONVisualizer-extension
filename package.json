{
  "name": "json-visualizer-extension",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:main": "vite build",
    "build:content": "vite build --config vite.content.config.js --mode content",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.0.4"
  },
  "dependencies": {
    "@components-1812/json-visualizer": "^0.0.2"
  }
}
